apiVersion: v1
kind: Service
metadata:
  name: telefon-rehberi-api-service
spec:
  type: NodePort
  selector:
    app: telefon-rehberi-api
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080